```bash
~/two-appApp
├── base
│   ├── deployment.yaml
│   └── kustomization.yaml
└── overlays
    ├── dev
    │   ├── deployment.yaml
    │   ├── namespace.yaml
    │   └── kustomization.yaml
    └── prod
        ├── deployment.yaml
        ├── namespace.yaml
        └── kustomization.yaml

```

At ~/two-app
```bash
controlplane:~~two-app$ k kustomize overlays/prod/ | kubectl apply -f -
namespace/prod-namespace created
deployment.apps/my-busybox-app created

controlplane:~two-app$ k kustomize overlays/dev/ | kubectl apply -f -
namespace/dev-namespace created
deployment.apps/my-busybox-app created
```