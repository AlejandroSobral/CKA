```bash
~/two-appApp
├── base
│   ├── deployment.yaml
│   └── kustomization.yaml
└── overlays
    ├── dev
    │   ├── deployment-patch.yaml
    │   └── kustomization.yaml
    └── prod
        ├── deployment-patch.yaml
        └── kustomization.yaml

```

At ~/two-app
```bash
k kustomize overlays/production | kubectl apply -f -
k kustomize overlays/dev | kubectl apply -f -
```